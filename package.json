{
  "name": "gitleb-website",
  "version": "1.0.0",
  "description": "GitLeb — Software Development & Automation company website",
  "scripts": {
    "dev": "vite --host",
    "build": "vite build",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
